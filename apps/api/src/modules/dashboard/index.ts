/**
 * Dashboard module barrel. Public surface the app composition
 * root wires: the router factory. The dashboard owns no collection.
 */
export { createDashboardRouter } from "@/modules/dashboard/routes.js";
